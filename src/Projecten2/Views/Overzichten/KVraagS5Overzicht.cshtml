@model Projecten2.Models.Domain.Analyse


<main class="overzicht">
    <div class="contain">
        <h1>Welke opleidingskosten moeten er gemaakt worden om de nieuwe kraachten op het gewenste niveau te krijgen?</h1>
        <a asp-controller="Vragen" asp-action="KVraagS4" asp-route-AnalyseId="@Model.AnalyseId" asp-route-VraagId="5" class="terug button"><i class="fa fa-plus"></i> Nieuwe type</a>
        <table style="width:30%">
            <tr>
                <th>Type</th>
                <th>Bedrag</th>
            </tr>
            @if (Model.Kosten.Kvragen5.Count == 0)
            {
                <tr>
                    <th colspan="2" class="alert">Er zijn geen functies.</th>
                </tr>
            }
            @foreach (var item in Model.Kosten.Kvragen5)
            {
                <tr>
                    <th>@item.Type</th>
                    <th>@item.Bedrag</th>
                </tr>
            }
        </table>
    </div>
</main>