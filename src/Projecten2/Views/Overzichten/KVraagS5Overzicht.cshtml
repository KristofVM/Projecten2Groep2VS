@model Projecten2.Models.Domain.Analyse


<main class="overzicht">
    <div class="contain">
        <h1>Welke opleidingskosten moeten er gemaakt worden om de nieuwe kraachten op het gewenste niveau te krijgen?</h1>
        <a asp-controller="Analyse" asp-action="kostenbaten" asp-route-id="@Model.AnalyseId" class="terug button"><i class="fa fa-arrow-left"></i> Terug</a>
        <a asp-controller="Vragen" asp-action="KVraagS4" asp-route-AnalyseId="@Model.AnalyseId" asp-route-VraagId="5" class="terug button"><i class="fa fa-plus"></i> Nieuw</a>
        <table style="width:30%">
            <tr>
                <th>Type</th>
                <th>Bedrag</th>
                <th></th>
            </tr>
            @if (Model.Kosten.Kvragen5.Count == 0)
            {
                <tr>
                    <th colspan="3" class="alert">Er zijn geen functies.</th>
                </tr>
            }
            @foreach (var item in Model.Kosten.Kvragen5)
            {
                <tr>
                    <th>@item.Type</th>
                    <th>€ @item.Bedrag</th>
                    <th><a asp-controller="Overzichten" asp-action="VerwijderKVraag" asp-route-VraagNr="5" asp-route-AnalyseId="@Model.AnalyseId" asp-route-VraagId="@item.Id"><i class="fa fa-trash"></i></a></th>
                </tr>
            }
        </table>
    </div>
</main>