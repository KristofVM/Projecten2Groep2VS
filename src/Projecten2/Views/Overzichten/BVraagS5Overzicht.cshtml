@model Projecten2.Models.Domain.Analyse


<main class="overzicht">
    <div class="contain">
        <h1>Welke kosten moet u ter voorbereiding maken om dit te implementeren?</h1>
        <a asp-controller="Vragen" asp-action="BVraagS1" asp-route-AnalyseId="@Model.AnalyseId" asp-route-VraagId="5" class="terug button"><i class="fa fa-plus"></i> Nieuwe type</a>
        <table style="width:30%">
            <tr>
                <th>Beschrijving</th>
                <th>Jaarbedrag</th>
            </tr>
            @if (Model.Baten.Bvragen5.Count == 0)
            {
                <tr>
                    <th colspan="2" class="alert">Er zijn geen functies.</th>
                </tr>
            }
            @foreach (var item in Model.Baten.Bvragen5)
            {
                <tr>
                    <th>@item.Beschrijving</th>
                    <th>@item.JaarBedrag</th>
                </tr>
            }
        </table>
    </div>
</main>